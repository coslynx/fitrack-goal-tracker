{
  "commands": [
    "npm install",
    "npm run build",
        "node startup.sh",
    "npm run test",
    "eslint --fix src/**/*.ts src/**/*.tsx",
        "prettier --write src/**/*.ts src/**/*.tsx"
  ]
}